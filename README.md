# Shapefile-Merger
Merges shapefiles for accessibility program
Instructions:
1. Put merge.bat, merger.py and main.py in the folder with all the different folder for each city
2. Click Merge.bat
3. If not present already a folder named "merged" will appear with the merged files